export type pageContent = {
  heading: string;
  message: string;
  footer: string;
  backButton: boolean;
};
export type donationOption = {
  id: number;
  amount: number;
};

export type donationType = {
  id: number;
  donationType: string;
};
